{
  "name": "whatsapp-bot",
  "version": "1.0.0",
  "description": "Bot de WhatsApp que envía mensajes automáticamente al grupo",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "whatsapp-web.js": "^1.0.0",  // Asegúrate de tener la versión más reciente de whatsapp-web.js
    "redis": "^4.0.0",  // Asegúrate de tener la versión más reciente de redis
    "dotenv": "^10.0.0",  // Si no lo tienes ya, puedes usar dotenv para gestionar variables de entorno
    "qrcode-terminal": "^0.12.0",  // Para mostrar el código QR en la terminal
    "axios": "^0.21.1"  // Si necesitas realizar solicitudes HTTP, como en el caso de la verificación de la sesión
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
